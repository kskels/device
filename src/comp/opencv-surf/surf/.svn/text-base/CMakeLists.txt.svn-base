
set(SURF_ROOT_DIR ${DEVICE_ROOT_DIR}/src/surf)
set(SURF_INCLUDE_DIR ${SURF_ROOT_DIR}/inc)
set(SURF_SOURCE_DIR ${SURF_ROOT_DIR}/src)

add_subdirectory(src)

add_custom_target(clean-surf
	rm -f ${SURF_ROOT_DIR}/inc/*.orig
	rm -f ${SURF_ROOT_DIR}/src/*.orig
)
